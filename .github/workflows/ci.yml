name: CI

on:
  # pull_request_comment is issue_comment ¯\_(ツ)_/¯
  issue_comment:
  pull_request_review_comment:
  pull_request_review:
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ci
        
      - run: |
          ./ci.bash
